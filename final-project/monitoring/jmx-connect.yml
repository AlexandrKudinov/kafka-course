startDelaySeconds: 5
hostPort: connect:9404
lowercaseOutputName: true
lowercaseOutputLabelNames: true
rules:
  - pattern: 'kafka.connect<type=(.+), name=(.+)><>(Count|Value)'
  - pattern: 'kafka.connect.mirror<type=(.+), name=(.+)><>(Count|Value)'
