groups:
  - name: kafka
    rules:
      - alert: KafkaBrokerJmxDown
        expr: up{job="kafka"} == 0
        for: 30s
        labels:
          severity: critical
        annotations:
          summary: Kafka broker metrics are unavailable
          description: JMX exporter for a Kafka broker is not reachable.
